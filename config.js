// 第一次提交到仓库,然后忽略把这个文件添加到 ignore 文件中
const secretKey = 'nodepy'
const mongodbName = 'node_test'
// 上传的文件
const uploadPath = 'uploads/'

module.exports = {
    secretKey: secretKey,
    mongodbName: mongodbName,
    uploadPath: uploadPath,
}